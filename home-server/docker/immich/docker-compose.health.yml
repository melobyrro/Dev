services:
  immich-server:
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:2283/api/server/ping | grep -q pong"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 60s
